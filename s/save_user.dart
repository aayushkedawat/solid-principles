import 'user.dart';

/// This class is responsible for storing the user's data into local/remote storage
/// If logic related to saving user's data changes, then it only this class will change

class SaveData {
  static void saveDataToLocal(User user) {
    // Logic to save user data to local
    print('Data is saved');
  }
}
